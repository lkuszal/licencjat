# description

def correct(enc_key):
    pass


def cipher(text, enc_key):
    if not correct(enc_key):
        return False


def decipher(text, enc_key):
    if not correct(enc_key):
        return False
    
    
if __name__ == "__main__":
    pass
